apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: app-staging
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: '1'
    spec:
      containers:
        - env:
            - name: TARGET
              value: Staging
          image: app
          name: app
