$extends:
  - app-service-base.yaml++
metadata:
  name: 'app-staging'
spec:
  template:
    spec:
      containers:
      - name: 'app'
        image: 'app'
        env:
          - name: 'TARGET'
            value: 'Staging'
